# DIA-NN-preprocess

This repository contains functions to pre-process protein group matrices (.pg_matrix.tsv) files generated by the DIA-NN software suite for data-independent acquisition (DIA) proteomics data.

